# Server-side encryption secret. Generate a long random value.
AI_NEXUS_SECRET="replace-with-a-long-random-secret"

# Optional bootstrap Gemini key. Prefer adding user-owned keys through the app.
GEMINI_API_KEY=""

# Optional server port.
PORT="3000"
